---
phase: 03-medium-modals-touch-polish
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - src/client/UI/MainUI.luau
autonomous: false
requirements: [TOUCH-03]

must_haves:
  truths:
    - "ScreenGui explicitly sets ScreenInsets to CoreUISafeInsets for documented safe area compliance"
    - "All 4 medium modals verified visually: fit within 414px viewport, buttons tappable, animation smooth, centered"
  artifacts:
    - path: "src/client/UI/MainUI.luau"
      provides: "Explicit ScreenInsets setting with documentation comment"
      contains: "ScreenInsets"
  key_links:
    - from: "src/client/UI/MainUI.luau"
      to: "Roblox ScreenGui.ScreenInsets API"
      via: "Enum.ScreenInsets.CoreUISafeInsets property set"
      pattern: "ScreenInsets"
---

<objective>
Verify safe area compliance (TOUCH-03) by making ScreenInsets explicit in code, then visually verify all 4 medium modals on mobile viewport.

Purpose: TOUCH-03 is primarily a verification task. Research confirmed the ScreenGui already defaults to CoreUISafeInsets, which protects against notch/Dynamic Island clipping. Making it explicit in code documents the decision and prevents accidental changes. The checkpoint confirms all Phase 3 modals work correctly on mobile.

Output: MainUI.luau updated with explicit ScreenInsets, human verification of all 4 modals.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-medium-modals-touch-polish/03-CONTEXT.md
@.planning/phases/03-medium-modals-touch-polish/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make ScreenInsets explicit for safe area compliance</name>
  <files>src/client/UI/MainUI.luau</files>
  <read_first>
    - src/client/UI/MainUI.luau (find ScreenGui creation, look for IgnoreGuiInset and any ScreenInsets setting)
  </read_first>
  <action>
    **TOUCH-03 verification:** Research confirmed ScreenGui defaults to CoreUISafeInsets when no explicit ScreenInsets is set. Make it explicit for documentation and safety.

    Find the ScreenGui creation in MainUI.luau (around line 58-61, where `screenGui.IgnoreGuiInset = false` is set). Add after `IgnoreGuiInset`:

    ```lua
    -- Safe area: CoreUISafeInsets prevents modal clipping behind notch/Dynamic Island (TOUCH-03)
    screenGui.ScreenInsets = Enum.ScreenInsets.CoreUISafeInsets
    ```

    This is a no-op change functionally (CoreUISafeInsets is already the default), but explicitly documents the decision and prevents future accidental changes.
  </action>
  <verify>
    <automated>grep -n "ScreenInsets" src/client/UI/MainUI.luau && grep -n "CoreUISafeInsets" src/client/UI/MainUI.luau</automated>
  </verify>
  <acceptance_criteria>
    - MainUI.luau contains `screenGui.ScreenInsets = Enum.ScreenInsets.CoreUISafeInsets`
    - MainUI.luau contains comment mentioning TOUCH-03 near the ScreenInsets line
  </acceptance_criteria>
  <done>ScreenGui explicitly sets CoreUISafeInsets for safe area compliance, documenting the TOUCH-03 decision in code</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all 4 medium modals on mobile viewport</name>
  <what-built>
    All 4 medium modals (RaceResultsUI, SummaryUI, MatchLobbyUI, StageSelectionUI) now have:
    - Responsive scaling via ResponsiveScale.applyToFrame
    - UIStroke compensation via compensateStrokes
    - Scale-in animation on show (0.8*target to target)
    - Touch target expansion on undersized buttons
    - Centered positioning with AnchorPoint(0.5, 0.5)
    - Safe area compliance via CoreUISafeInsets
  </what-built>
  <how-to-verify>
    1. Open Roblox Studio with the game place
    2. Run `rojo serve` and connect Roblox Studio
    3. Enable Device Emulator in Studio (View > Device Emulator)
    4. Select iPhone SE (414px wide) or similar small phone
    5. Play-test the game and verify each modal:

    **RaceResultsUI:**
    - Start a match with 2+ players (or use testing menu if available)
    - When match ends, verify RaceResultsUI fits on screen, no content clipped
    - Verify Continue button is tappable (visually not too small)
    - Verify scale-in animation plays smoothly (subtle pop-in)

    **SummaryUI:**
    - Complete a race to trigger SummaryUI
    - Verify modal scales in with smooth animation (no double-scale flicker)
    - Verify OK button is tappable
    - Verify the modal hides with smooth scale-out animation
    - If gamepass upsell shows, verify those buttons are tappable too

    **MatchLobbyUI:**
    - Open matchmaking lobby
    - Verify modal fits on screen
    - Verify X close button is visually acceptable at 56x56 size
    - Verify Leave/Start buttons are tappable
    - Verify player list scrolls correctly

    **StageSelectionUI:**
    - Open stage selection
    - Verify modal is centered (no offset glitch)
    - Verify stage buttons grid is visible and tappable
    - Verify Clear, Random, Start buttons are tappable
    - If room join banner shows, verify Join button is tappable
    - Verify modal fits on screen (this one scales most aggressively at 0.58)

    6. Switch to a desktop/large viewport and verify all 4 modals show at full original size
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 modals look correct, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- grep confirms ScreenInsets = CoreUISafeInsets in MainUI.luau
- Human verifies all 4 modals visually in Roblox Studio device emulator
</verification>

<success_criteria>
1. MainUI.luau explicitly sets ScreenInsets = Enum.ScreenInsets.CoreUISafeInsets
2. Human confirms all 4 medium modals fit within 414px phone viewport
3. Human confirms buttons are tappable at scaled sizes
4. Human confirms scale-in animation is smooth
5. Human confirms modals remain centered
6. Human confirms desktop viewport shows original sizes
</success_criteria>

<output>
After completion, create `.planning/phases/03-medium-modals-touch-polish/03-03-SUMMARY.md`
</output>
